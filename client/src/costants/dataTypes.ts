export type loginType = {
    email: string;
    password: string;
};

export type registerType = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};

export type forgetPassordType = {
    email: string;
};